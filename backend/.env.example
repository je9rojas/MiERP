# ==============================================================================
# PLANTILLA DE VARIABLES DE ENTORNO PARA EL BACKEND (.env.example)
#
# INSTRUCCIONES:
# 1. NO edites este archivo directamente.
# 2. Copia este archivo y renómbralo a ".env" en la misma carpeta (/backend).
# 3. Rellena las variables en tu archivo .env con tus secretos reales.
# 4. El archivo .env NUNCA debe ser subido a un repositorio de Git.
# ==============================================================================


# --- SECCIÓN 1: CONFIGURACIÓN GENERAL DEL ENTORNO ---
# Entorno de ejecución: "development" para desarrollo local, "production" para Render.
ENV="development"


# --- SECCIÓN 2: BASE DE DATOS (OBLIGATORIO) ---
# URI de conexión completa a tu clúster de MongoDB Atlas.
# Asegúrate de reemplazar los placeholders y de que el nombre de la BD esté en la URI.
DATABASE_URL="mongodb+srv://<TU_USUARIO_DE_BD>:<TU_CONTRASEÑA_DE_BD>@<TU_CLUSTER_URL>/<TU_NOMBRE_DE_BD>?retryWrites=true&w=majority"


# --- SECCIÓN 3: SEGURIDAD Y JWT (OBLIGATORIO) ---

# Clave secreta para la firma de tokens JWT. DEBE SER SECRETA Y ÚNICA.
# Para generar una nueva, ejecuta en tu terminal: openssl rand -hex 32
SECRET_KEY="<UNA_CLAVE_SECRETA_GENERADA_DE_64_CARACTERES>"

# Orígenes del frontend permitidos para desarrollo local.
# Para producción (Render), esto se configura en la interfaz de Render
# con la URL de tu frontend (ej. '["https://tu-frontend.vercel.app"]').
ALLOWED_ORIGINS='["http://localhost:3000", "http://127.0.0.1:3000"]'

# Duración del token de acceso en minutos.
ACCESS_TOKEN_EXPIRE_MINUTES=90
ALGORITHM="HS256"


# --- SECCIÓN 4: CREDENCIALES DEL SUPERADMINISTRADOR INICIAL (OBLIGATORIO) ---
# Estas credenciales se usarán para crear el primer usuario con todos los permisos.
SUPERADMIN_EMAIL="admin@example.com"
SUPERADMIN_PASSWORD="<UNA_CONTRASEÑA_FUERTE_Y_SEGURA_PARA_EL_SUPERADMIN>"