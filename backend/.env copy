# backend/.env 
# ==============================================================================
# VARIABLES DE ENTORNO PARA DESARROLLO LOCAL DEL BACKEND
#
# Este archivo contiene secretos y configuraciones para el entorno de desarrollo.
# NO debe ser subido a repositorios de Git.
# ==============================================================================


# --- SECCIÓN 1: CONFIGURACIÓN GENERAL DEL ENTORNO ---
# Define el entorno de ejecución. Opciones: "development", "production".
ENV="development"


# --- SECCIÓN 2: BASE DE DATOS (OBLIGATORIO) ---
# URI de conexión completa a MongoDB Atlas.
DATABASE_URL="mongodb+srv://db_admin:dQ6n5znkCVO0ANm6@erp-cluster.fzyhb.mongodb.net/midb?retryWrites=true&w=majority&appName=ERP-cluster"


# --- SECCIÓN 3: SEGURIDAD Y JSON WEB TOKENS (OBLIGATORIO) ---

# Clave secreta para la firma de tokens JWT.
# ¡IMPORTANTE! Esta es una clave de ejemplo. Genera la tuya para un proyecto real.
# Comando para generar una nueva: openssl rand -hex 32
SECRET_KEY="1d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1e"

# Orígenes (URLs del frontend) que tienen permiso para acceder a esta API.
# ALLOWED_ORIGINS= "["https://URL-DE-TU-FRONTEND.vercel.app"]"
ALLOWED_ORIGINS='["http://localhost:3000", "http://127.0.0.1:3000"]'

# Duración del token de acceso en minutos.
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Algoritmo de firma para los tokens JWT.
ALGORITHM="HS256"


# --- SECCIÓN 4: CREDENCIALES DEL SUPERADMINISTRADOR INICIAL (OBLIGATORIO) ---
# Estas variables son requeridas por la aplicación para crear el primer usuario
# con todos los permisos durante el arranque inicial.

SUPERADMIN_EMAIL="super_admin@dirogsa.com"
SUPERADMIN_PASSWORD="kR8#z!9p$L@w2vF&bQ"